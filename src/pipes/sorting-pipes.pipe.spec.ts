import { SortingPipesPipe } from './sorting-pipes.pipe';

describe('SortingPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortingPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
